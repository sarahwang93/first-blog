export interface Task {
  id: string, 
  userId: string,
  title: string,
  summary: string
}

export interface NewTaskData{
    id: string, 
    userId: string,
    title: string, 
    summary: string
}