import { AppReadDirective } from './app-read.directive';

describe('AppReadDirective', () => {
  it('should create an instance', () => {
    const directive = new AppReadDirective();
    expect(directive).toBeTruthy();
  });
});
